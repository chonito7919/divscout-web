{
  "name": "divscout-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for DivScout web application",
  "scripts": {
    "test": "playwright test smoke.spec.js --project=chromium",
    "test:full": "playwright test e2e.spec.js --project=chromium",
    "test:smoke": "playwright test smoke.spec.js --project=chromium",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:chromium": "playwright test --project=chromium",
    "test:firefox": "playwright test --project=firefox",
    "test:webkit": "playwright test --project=webkit",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
